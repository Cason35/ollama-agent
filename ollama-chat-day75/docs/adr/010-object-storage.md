# ADR-010：使用对象存储管理大文件

## Problem
知识文档、附件、导出结果和备份不适合直接存入关系型数据库或应用磁盘。
## Decision
通过 Storage Provider 抽象使用 MinIO，数据库只保存对象键、校验和与权限元数据。
## Alternatives
数据库 BLOB、本地文件系统、直接绑定特定云厂商 SDK。
## Trade-off
获得大对象扩展性和云兼容性，但增加对象与元数据一致性、生命周期和权限管理。
## Consequence
上传先校验租户和类型，删除采用可恢复流程，备份必须同时覆盖数据库和对象存储。
